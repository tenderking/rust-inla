[package]
name = "r-inla"
version = "0.1.0"
license = "Apache-2.0"
repository = "https://github.com/tenderking/rust-inla"
edition = "2024"

[lib]
crate-type = ["cdylib"]
name = "inla_rs"

[dependencies]
extendr-api = "0.9"
inla_core = { path = "../crates/inla_core", features = ["sparse-ldlt"] }

[lints]
workspace = true
