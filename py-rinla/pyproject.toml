[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rinla"
requires-python = ">=3.8"
version = "0.1.0"
description = "Fast approximate Bayesian inference using INLA, written in Rust"

[tool.maturin]
features = ["pyo3/extension-module"]
