[package]
name = "inla_core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[features]
default = []
gmrflib-ffi = ["dep:inla_sys"]

[dependencies]
sprs = "0.11"
rayon = "1"
inla_sys = { path = "../inla_sys", optional = true }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "ar1_ldlt"
harness = false
