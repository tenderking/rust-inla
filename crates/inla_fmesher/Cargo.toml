[package]
name = "inla_fmesher"
version = "0.1.0"
edition = "2024"
description = "Mesh generation, topology, point location, and FEM block assembly (legacy fmesher)"

[lib]
crate-type = ["rlib"]

[dependencies]
